Fix the following {language} code. The compiler error is:
{error_type}

Output ONLY in this exact format, nothing else:

ERROR:
[one line describing the bug]

FIXED CODE:
```
[complete fixed code only]
```

Code to fix:
{code}